# Popup System
- A flexible system for managing UI popups with support for async loading, stacking, and customizable animations.

## 🚀 Features

### ✅ Completed
- Preload Support: Preload UI prefabs to reduce runtime latency.
- On-Demand Loading: Load and spawn UI components only when required.
- On-Demand Cleanup: Unload and destroy UI assets when required to free up memory.
- Extensible Animations: Support for customizable show and hide animations.
- Async Support: Asynchronous loading and instantiation.
- Addressable Integration: Only loads UI prefabs from Addressables for better memory management.
- Initialization Guard: Ensures UI is fully initialized before being displayed.
- Auto Layering: Automatically stacks popups so the newest appears on top.

### ⏳ Planned (To-Do)
- Loading Indicators: Display a spinning circle while internal content data loads.

### ❌ Out of Scope / Not Implemented
- Nothing at this time.